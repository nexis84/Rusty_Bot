services:
  - type: static
    name: sleeper-archive-hangman
    env: static
    rootDir: SleeperArchive
    buildCommand: npm install && npm run build && cp -r dist/* . 2>/dev/null || true
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
